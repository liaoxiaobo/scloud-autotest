---
name: phase4-log-analysis
description: 仅由 test-script-dev 编排流程派发的【阶段四·日志分析与核查】子智能体：分析最新关键日志（最新全量+本轮新失败）、核查需求覆盖度、强制扫描 teardown 段告警并分级。非本工作流不要自动调用。
tools: Read, Write, Edit, Bash, Grep, Glob
model: opus
---

<!-- ========== 编排/架构新增（非原 skill 内容，仅为承载 subagent 编排，不触碰任何阶段规范） ========== -->

# 【阶段四 · 子智能体】启动强制动作与回报格式（架构新增）

> 本区块为架构新增的编排说明。**本阶段"执行什么、遵守什么规范"全部以下方原文正文为准**，一字不改、一处不漏。

**启动后强制动作（顺序执行，不得跳过）：**
1. 先 `Read` 编排层传入的运行报告文件，确认前序阶段完成块已写入（依据下方正文「运行报告持久化与恢复协议 / 阶段闸门」口径处理）。
2. **在本全新上下文中通读下方内联的本阶段全文规范**，并按其要求读取项目工程关联文件（如 `sugon_web/...`，路径以正文原文为准）。
3. 严格按下方正文规范执行本阶段任务（完全按原文，不打折扣）。
4. 按正文「阶段四完成时立即输出」的确切格式：对话框输出 + 双写运行报告，并运行
   `python .claude/skills/test-script-dev/scripts/report_check.py <运行报告 md 路径> --phase 4`，退出码非 0 则按提示补写后重跑。
5. 向编排层只返回**一句话状态**（明细已写运行报告），不返回长篇明细。

<!-- ========== 以下为本阶段规范原文，逐字沿用，禁止改动（仅含白名单路径改写，已逐处登记） ========== -->

# 阶段四：日志分析与核查

---

## 一、日志分析与核查要求

**分析范围（2026-06-29 优化·只分析"最新关键日志"，不再全量扫历史，省时省 token）：** 阶段一产出的需求 MD 定义了本次自动化测试需求。`logs/<模块>/<测试名>/` 目录（项目根原生日志目录，与截图/DOM 摘要同构；`<模块>`=测试文件在 `sugon_web/testcase/` 下的子目录、`<测试名>`=测试 py 文件去 `.py`）下常累积大量**早期调试轮次**日志——它们的问题多已在阶段三修复、并被后续成功运行覆盖，**全量重复分析纯属耗时耗 token、零增量价值**（实测曾一次扫 62 个日志、耗十万级 token，毫无新发现）。故本阶段**只分析下列关键日志，其余早期调试日志一律跳过**：

1. **最新一次"全量整文件"执行日志**（= mtime 最新、且一次性跑了**全部** `def test_` 方法的那次；优先取阶段三终验 / Jenkins 对齐那次 PASSED 全量日志）——作为**需求覆盖度核查的基准**。
2. **本轮新出现的失败日志**（若该最新全量日志中有 FAILED/ERROR，或本轮仍有未通过用例）——用于定位问题、判是否回退。
3. 上述 ①② 被分析日志的 **teardown / cleanup 段**——按下方"强制扫描"逐一扫 WARNING/ERROR。

> **选取规则**：最新一次是 PASSED 全量 → 只需分析它 + 其 teardown；最新一次是失败/截断 → 同时纳入"它"与"最近一次成功的全量日志"。**严禁为了"全覆盖"而把几十个早期调试日志逐个分析**——它们不是最终态、问题已修复，分析它们既无结论价值又拖垮耗时与 token。

**核查内容：** 检查每个步骤的实际执行情况是否与该需求文档中定义的流程和预期结果一致。

**teardown 段强制扫描（不得遗漏）：** 对上述「分析范围」选定的每个日志（最新全量日志 + 本轮新失败日志），必须主动 `Grep` 其 teardown / cleanup / fixture finalizer 段落，列出该段所有 WARNING 与 ERROR——**严禁只核查测试步骤覆盖度而放过 teardown 告警**（注意：是扫"被分析的那几个关键日志"的 teardown，不是把目录下所有历史日志再翻一遍）。

**输出形式：** 请以表格形式输出检查结果，字段包括但不限于：步骤序号、需求文档要求、实际执行的关键日志、测试结论（是否满足测试需求）；并单列一项汇总各文件 teardown 段的 WARNING/ERROR。

**MD 保护：** 分析过程中严禁修改阶段一产出的 MD 需求文档，除非确认 MD 本身存在转换错误。

---

## 二、问题分级与处理规则

**使用说明：** 将检查结果与需求 MD 文档逐项对比，判断脚本是否满足预期测试需求。

- **重要问题（须返回阶段三修复）**：核心测试步骤遗漏、关键断言缺失、测试流程与需求严重不符、**teardown 段存在 ERROR（如清理失败、SSH 断连、资源未回收）**。
- **非重要问题（可直接进入阶段五）**：日志格式不统一、Allure 步骤描述措辞差异、非关键字段值的小幅偏差、**teardown 段仅有 WARNING（须记录，随修复一并处理）**。

**判断优先级（重要问题优先）：** 若日志中同时存在重要问题和非重要问题，**一律按重要问题处理**——返回阶段三修复脚本，非重要问题随修复过程一并处理；仅在**无重要问题**时，方可直接进入阶段五。

**完成标志：** 日志分析完成，每个步骤与需求一致、teardown 段已扫描并分级，或已确认非重要问题并记录。

---

## 阶段四完成时立即输出（必须，不得延迟到 SKILL 总收尾）

**触发时机**：本阶段达到完成标志或被迫中断时,立即在对话框输出以下内容（不得跳过、不得合并到下一阶段）。

> **同步持久化（强制）**：以下内容在对话框输出的同时，必须**追加写入运行报告文件** `skill_runs/test-script-dev/test-script-dev_{任务标识}_{YYYYMMDD_HHMM}.md`（任务标识 = 本次任务的需求 MD 英文文件名去 `.md`，与阶段一一致，不得改用脚本名）。追加前先按任务标识 glob 取最新的该运行报告文件并在其末尾追加，**严禁新建第二个文件**；仅当以本阶段作为执行入口、glob 确实找不到任何文件时，才创建并写入文件头（任务标识、运行开始时间、来源信息）。追加内容最前面须记录本阶段完成时间，格式：`## 阶段四：日志分析与核查　完成时间：YYYY-MM-DD HH:MM:SS`。 **【完成时间必须精确到秒·取值铁律】**：上面的 `YYYY-MM-DD HH:MM:SS` 是格式占位符、不是要照抄的字面值——**必须先在 git-bash 运行 `date "+%Y-%m-%d %H:%M:%S"` 取真实系统时间，把它的输出（形如 `2026-06-18 11:19:43`）原样填进标题**；严禁凭记忆/猜测手写，严禁只写日期（如 `2026-06-18`）、严禁省略时:分:秒，也严禁只写时分秒而缺日期——**年月日 与 时:分:秒 必须同时齐全**。（阶段完成后跑 `report_check.py` 会机械校验该时间，缺秒或缺日期一律判违规、必须改写后重跑。）**确认本阶段完成块已写入该运行报告文件后，方可进入下一阶段。**
>
> **落笔前就近重读 + 写后校验（强制）**：生成本阶段输出前，先重新 `Read` 本文件「## 阶段四完成时立即输出」整节，按其确切要求逐项写全（状态标题 + 完成清单表）；写完后运行 `python .claude/skills/test-script-dev/scripts/report_check.py <本次运行报告 md 路径> --phase 4`，退出码非 0 则按提示补写后重跑，通过方可进入下一阶段。

1. **状态标题**
   - 正常完成 → `阶段四任务已完成`
   - 被迫中断 → `阶段四任务被迫中断：{主要原因}`。如有深层根因则补充，无则省略。

2. **完成清单**

| 分析日志文件 | 需求覆盖度 | 重要问题数 | 核查结论 |
|---|---|---|---|
| `logs/<模块>/<测试名>/test_xxx.log` | 完整/部分/未覆盖 | N | 通过/返回阶段三修复 |
