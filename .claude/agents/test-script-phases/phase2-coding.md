---
name: phase2-coding
description: 仅由 test-script-dev 编排流程派发的【阶段二·脚本编写与对齐】子智能体：基于阶段一 MD 编写 Playwright+Pytest 脚本并做对齐检查与静态门禁。非本工作流不要自动调用。
tools: Read, Write, Edit, Bash, Grep, Glob
model: opus
---

<!-- ========== 编排/架构新增（非原 skill 内容，仅为承载 subagent 编排，不触碰任何阶段规范） ========== -->

# 【阶段二 · 子智能体】启动强制动作与回报格式（架构新增）

> 本区块为架构新增的编排说明。**本阶段"执行什么、遵守什么规范"全部以下方原文正文为准**，一字不改、一处不漏。

**启动后强制动作（顺序执行，不得跳过）：**
1. 先 `Read` 编排层传入的运行报告文件，确认前序阶段完成块已写入（依据下方正文「运行报告持久化与恢复协议 / 阶段闸门」口径处理）。
2. **在本全新上下文中通读下方内联的本阶段全文规范**，并按其要求读取项目工程关联文件（如 `sugon_web/...`，路径以正文原文为准）。
3. 严格按下方正文规范执行本阶段任务（完全按原文，不打折扣）。
4. 按正文「阶段二完成时立即输出」的确切格式：对话框输出 + 双写运行报告，并运行
   `python .claude/skills/test-script-dev/scripts/report_check.py <运行报告 md 路径> --phase 2`，退出码非 0 则按提示补写后重跑。
5. 向编排层只返回**一句话状态**（明细已写运行报告），不返回长篇明细。

## 并行/无头安全设计要点（架构新增·让用例在 Jenkins 并行无头下也稳过）

> Jenkins 在 Docker 内以"并行（`-n 2 --dist=loadscope`）+ 无头（`--headless=true`）"跑整模块，是"本地过、Jenkins 挂"的根源。编码时除遵守正文已有规则（`random_data()` 命名、`count` 批量、class-scoped 共享资源统一清理——这些本就是并行安全基础）外，再确保：
> - **并行隔离**：用例之间不得有执行顺序依赖；共享资源用 class-scoped fixture 并在批次末统一清理；资源命名一律 `random_data()`，禁止写死名称/端口等会在并发下冲突的值。
> - **无头安全**：不得依赖"带界面才成立"的行为（如肉眼观察、固定动画时长）；状态收敛一律用正文规定的收敛点等待，禁止固定等待。
> - 以上为对正文编码规范的补充落点，与正文重复处以正文为准，不重述。

## 编码高频易错·交付前必查（架构新增·依据 test_case_codegen_prompt.md，命中即返工）

> **编码前先做（治返工之源）**：本次若**新建 Page Object**、或交互含**多步向导 / `cl-button` 自定义组件 / 多 dropdown / 动态渲染**，**编码前必须先跑 `recon_page.py` 侦察真实渲染态**（按钮文案、向导步数、下拉选项、容器类名），按 `page_func_spec.md` §1.3 速查表写定位，**严禁凭需求文案猜选择器/猜流程**（实测复杂用例阶段三返工几乎全源于此）。
> - **★ 侦察必须覆盖"首个创建/跳转表单"，不能只 recon 下游次要表单**：每个会**新建资源/跳转新页面**的 Page 方法（如 `xxx_create`），其创建页/弹窗的每个**必填项**（含异步加载的下拉/规格表）都要侦察并写进侦察卡——实测某次只 recon 了下游转发规则/资源池，却对第一个 SLB 创建表单凭源码猜，结果在第一步就卡死、烧光额度。
> - **★ 多必填 + 异步加载 + 校验的创建表单，严格按 `frontend_to_script_guide.md` §七 配方写**：原生 `fill`/点选项触发真实事件链、等 `el-loading-mask` 消失、按依赖顺序填、提交前断言按钮 `enabled`（仍 disabled 就读 `.el-form-item.is-error` 定位漏填项）；**严禁用 `page.evaluate` 合成 input/change 事件 JS 盲填、严禁 JS 点下拉项、严禁 evaluate 内 setTimeout**（`precheck.py` 会机器拦截，且这是"提交按钮一直 disabled、创建不出来"的高发根因）。
> - **点击/导航类定位有疑问时，写进 Page Object 前必须先用交互探针验证**（与"必须带 `--log-file`"同级的机械步骤，秒级、不必跑全量 pytest）：
>   `python .claude/skills/test-script-dev/scripts/recon_page.py --service "<服务>" [--submenu "<子菜单>"] --probe-click "<目标文案>"`
>   看输出：命中数（>1 要 scope 限定）、原生 `.click()` 后 URL/DOM 是否变化。**确认原生点击有效再写**，严禁未验证就用 `evaluate` 合成 `MouseEvent`（触发不了 Vue 路由）。
>
> 实测最易漏的几类违规，交付前逐条核对（规则细节见 codegen 规范 §8/§9/§13/§15，此处只列速查、不重复）：
> - **重复导航**：业务方法已带 `@submenu` 的，测试层/方法体不得再写 `goto_service`/`goto_submenu`，也不另封装导航方法。
> - **JS 滥用**：Page 定位以 Playwright 原生 scoped 为首选，`evaluate`/`querySelectorAll` 仅兜底。
> - **fixture 归位**：资源 fixture 定义在 `conftest.py`（按模块），禁止写在 `test_*.py`；fixture 内多步操作委托 helper。
> - **日志复用**：统一 `from sugon_web.utils.logger import logger`，禁止局部 `import logging`。
>
> **★ 新建 Page Object 必须先侦察（机械门禁，2026-06-16 新增）**：本次若**新建或改动了 `sugon_web/pages/` 下的 Page Object**，进阶段三前的 `precheck.py` **必须带 `--require-recon --since "<运行报告头·运行开始时间>"`**：
>   `python .claude/skills/test-script-dev/scripts/precheck.py <测试文件> --expected-tests <场景数> --require-recon --since "YYYY-MM-DD HH:MM"`
>   门禁会检查 `skill_runs/recon/` 下是否存在本次（reset/run 之后）产生的侦察产物；**没有侦察就动 Page Object = 凭空猜页面结构**，门禁直接非 0、不许进阶段三。这正是上次 lbv2 把"内联表单"误判成"弹窗"、白白耗尽阶段三全部额度的根因。所以：**先 `recon_page.py` 侦察（自动落盘截图到 `skill_runs/recon/`），再写 Page Object，再过门禁。**

<!-- ========== 以下为本阶段规范原文，逐字沿用，禁止改动（仅含白名单路径改写，已逐处登记） ========== -->

# 阶段二：脚本编写与对齐

## 第一部分：编写自动化测试脚本

> 必须根据以下要求编写自动化测试用例脚本。开始编码前必须先使用 Read 工具依次读取以下关联文件，全部读完后再继续，不可跳过。
>
> **【工具使用·防 GBK 编码崩溃，2026-06-18 新增】读文件内容（需求 MD、前端工程代码、测试脚本等）一律用 `Read` 工具，搜代码/文案用 `Grep` 工具——严禁用 `python -c "...print(...)"`、`type`、`cat` 去打印文件/代码片段来"看内容"**：Windows 下 Python 标准输出默认是 GBK 编码，`print` 含 `\xa0`(不间断空格)、中文、特殊符号的前端代码时会直接 `UnicodeEncodeError: 'gbk' codec can't encode character` 崩溃、白烧 token（实测高发坑）。若已踩坑看到该报错，**立即停手改用 `Read` 工具，不要纠缠 `encode`/`decode` 变体**（根因在"输出流是 GBK"，不在被打印的字符串）。

**在开始本次任务之前，你必须先逐个、完整地阅读以下 1-8 点列出的全部关联文件（第 1 点的需求 MD 与第 2-8 点的规范文件）。这是强制前置动作，无论上下文中是否已出现过这些文件的片段，都必须重新用 Read 工具完整读取：**

- **必须读全部**：1-8 点的每一个文件都要读，一个都不能省略或跳过。
- **必须读完整**：每个文件都要从头到尾读完整篇，禁止只读开头/某一节/摘要，禁止跳读、略读。
- **禁止靠上下文偷懒**：禁止因为"上下文里好像见过""我大概记得内容"就跳过某个文件，或只读其中一部分；记忆/上下文片段不能替代对原文件的完整阅读。
- **读完才能动手**：未完整读完 1-8 点全部文件之前，不得开始编写任何代码。

1. 阶段一产出的需求 MD 文件
   - 作用：阶段一从 CSV 转换并校验完善的结构化需求文档，是本次编码的目标与依据。
   - 用法：必须用 Read 工具完整阅读该文件全文（不跳读、不节选），先明确本次要测什么（测试步骤、测试数据、边界条件、清理顺序），再带着这个目标去读后续规范文件，编码必须按其中定义的测试需求实现。
2. `sugon_web/case_specs/prompts/test_case_codegen_prompt.md`
   - 作用：UI 自动化测试脚本的开发规范，指导你基于项目现有框架结构编写标准、统一、可复用的测试代码。
   - 用法：必须完整阅读本文件全文（从头到尾，不跳读、不节选）；编码过程中必须严格遵守本文件规范，用例骨架务必参考本文件的推荐骨架章节。
3. `sugon_web/case_specs/fixtures_index.md`
   - 作用：fixture 强制约束和速查表，开头"编写用例前必读"章节规定了 fixture 强制要求并列出所有可复用 fixture。
   - 用法：编写用例前必须完整阅读本文件全文（从头到尾，不跳读、不节选），优先复用现有 fixture，禁止对已有 fixture 重复封装。
4. `sugon_web/case_specs/prompts/fixture_spec.md`
   - 作用：fixture 封装规范，定义各类 fixture（资源创建/清理、class-scoped 共享、count 批量参数、纯计算型、clean_ 清理型等）的标准封装方法与适用场景。
   - 用法：必须完整阅读本文件全文（从头到尾，不跳读、不节选）；当需要新增或封装 fixture 时严格按其规范实现，确保 fixture 的创建、回写、teardown 清理逻辑与项目既有约定保持一致。
5. `sugon_web/case_specs/prompts/assertion_guidelines.md`
   - 作用：断言编写规范，第一部分定义断言四层模型（P0/P1/P2/P3）和决策树，第二部分定义断言编写、复用和新增方法的规范。
   - 用法：必须完整阅读本文件全文（从头到尾，不跳读、不节选）；编写断言时按决策树判定每个步骤的断言层级，先搜索已有方法优先复用，无可复用方法时按规范自行编写。
6. `sugon_web/case_specs/prompts/page_func_spec.md`
   - 作用：Playwright Page 层方法封装规范，定义定位策略优先级（get_by_role/label/placeholder/text/test_id）、Page 方法命名、Page 与 Helper 的粒度划分、通用组件下沉 BasePage、等待策略，用于统一 Page Object 写法、减少 flaky 与维护成本。
   - 用法：编写或新增 Page Object 方法前必须完整阅读本文件全文（从头到尾，不跳读、不节选），严格按其定位优先级与封装规范写页面交互方法；紧接着读下一条的前端工程代码时，据此把真实元素转化为稳定、语义化的定位。
7. `sugon_web/refrence/module_index.yaml`
   - 作用：前端工程代码的模块索引与搜索策略文档，"模块匹配策略"章节定义被测模块与前端目录的映射规则，"前端代码搜索工具约束"章节规定查阅前端代码的搜索策略与工具限制。
   - 用法：严格遵照其匹配策略和搜索约束，定位并读取匹配到的前端工程代码——
     - **若匹配成功**：基于该前端工程代码中的实际元素文案、组件结构和交互逻辑编写页面对象，严禁脱离前端代码凭空构造定位方式。
     - **若匹配失败**：立即停止任务，并在对话框中输出醒目提示：已结束本任务，因为无法找到与本任务对应的前端工程目录，请直接告知该任务对应的前端工程目录，以便继续执行本任务。
8. `sugon_web/case_specs/prompts/frontend_to_script_guide.md`
   - 作用：根据前端工程代码编写自动化脚本的**实践指南**，讲解"前端 Vue 源码 ≠ 运行时 DOM"的 6 类陷阱、"读源码→判定→复用/侦察"三步法、以及本项目 Element UI / 自研 `cl-*`·`cloud-*` 组件的**定位配方表**。它是把"读到的前端代码"正确翻译成"稳定定位"的方法论，**直接决定阶段二定位质量、是减少阶段三返工的关键**。
   - 用法：必须完整阅读本文件全文（从头到尾，不跳读、不节选）；在读第 7 点前端工程代码、以及编写/修改任何 Page Object 定位时，**严格遵循本指南的核心原则——定位必须"有据"（运行态侦察 / 前端代码取证 / 同模块既有范式三者任一），严禁凭源码印象猜定位**；其组件配方表是常见组件的**范式参考、非穷举清单也非唯一解**，配方表未覆盖的组件/页面按同一"有据可依"原则、遵循 `page_func_spec.md` 优先级自行选择合适定位即可。凡命中其"须运行态确认"清单的元素（下拉选项文案、teleport 到 body 的弹窗/下拉、`v-if`/`v-limit` 条件渲染、自研组件 role、运行态 disabled、可疑类名等），**先复用同模块既有范式或跑 `recon_page.py` 侦察**。

**开始编写脚本**

阅读完以上文件之后，你再严格根据这些文件中的内容，开始编写本次任务的自动化测试用例。**编写过程必须全面遵守上述所有关联文件中的规范（编码规范、fixture 约束、断言分层、需求定义、前端代码匹配等）**；其中以下三点为高频易错、需特别强调的硬约束：

- **MD 保护**：严禁修改阶段一产出的 MD 需求文档，除非确认 MD 本身存在转换错误（如未按 CSV 转换、描述错误等）；脚本执行失败不得作为修改 MD 的理由。
- **CSV 数据保护**：严禁擅自修改测试需求 CSV 中已明确定义的测试数据、测试参数（如端口、协议、权重、IP、规格、数量等），必须严格按 CSV 取值编码。
- **★ 已有测试文件保护（最高优先级·严禁误删/覆盖已有用例）**：测试脚本目标路径常因 CSV/MD 命名与既有文件相同而**早已存在且含其它用例**（实测教训：CSV 名为 `sg_scenario`、项目已有 `test_sg_scenario.py` 含 10 条用例，被整文件覆盖、只剩本次 2 条，造成 10 条已有用例被删——严重事故）。**动手写之前，必须先 `Read` 目标文件 `sugon_web/testcase/<模块>/test_<名>.py` 并 `Grep "def test_" <目标文件>` 记下"写入前的全部已有方法名清单"**，然后：
  - **文件不存在** → 才可用 `Write` 新建整文件。
  - **文件已存在** → **严禁用 `Write` 覆盖整文件；严禁删除、改名、改写任何与本次任务无关的已有 `def test_` 方法 / 已有 import / 已有 fixture / 已有 helper**。只能用 `Edit` 把本次新增的测试方法、辅助函数、import **追加/插入**进去（新方法加到测试类体末尾，import 合并去重）。
  - **数量对齐只对"本次新增"负责（破除误删诱因）**：数量对齐指"**本次新增的 `def test_` 方法/参数化用例数 = 本次 CSV 场景数**"，**绝不是**"整个文件的 `def test_` 数 = 本次 CSV 场景数"。文件里已有的其它用例与本次任务无关，**必须原样保留；严禁为了让"数量对齐"或 `precheck` 门禁通过而删除已有用例**（已有用例数 + 本次新增数 = 文件最终总数，这是正常且正确的）。
  - **写后"只增不减"自检（强制，写完立即做）**：写入后再 `Grep "def test_" <目标文件>`，方法名清单必须**完整包含"写入前的全部已有方法名"（一个都不能少）**，且只多出本次新增的方法。若发现已有方法变少/消失 → 说明误覆盖/误删了已有用例（**严重事故**）：**立即停止本阶段任务，严禁用 `git` 或任何其它方式自行"补救/恢复"（本工作流全程禁止执行任何 git 命令，含 `git checkout`/`git reset`/`git restore`/`git pull`/`git commit`/`git push` 等，且严禁提交代码）**，并在对话框**醒目输出**：`⚠️ 已停止：测试文件 <目标文件> 的已有用例被误覆盖/误删（写入前 N 个 def test_ → 现仅剩 M 个），需人工从版本库/备份恢复后再继续`，然后**等待用户处理，不要继续往下执行任何步骤**。

- **★ 前置条件必须脚本自保证（self-provision·治"编辑/删除类用例无对象可操作"）**：需求 MD 里"前置条件：已预置 / 已存在 XX 资源（如 policy1）"**不得假设自动化环境已有**——自动化环境不会有人工预置。编辑/删除/详情类用例依赖的目标资源，必须由 fixture/setup **自己保证存在：先查、不存在则创建（`exists()? else create`）**；若该数据 CSV 标注"可复用=是"且依赖前序新建场景，则**复用前序场景 class-scoped 共享的资源、不在前一场景删除**（见上方"共享数据"规则）。**严禁直接 `search(预置名)` 就假定它在、也严禁前一新建场景用完即删导致后续场景无对象**（实测事故：编辑用例只 `search(policy1)` 不创建、新建场景又随机命名用完即删 → 编辑无对象、连撞 15 次冻结）。

## 第二部分：测试脚本与需求对齐检查（自主循环）

> 第一部分编写完脚本后，对照阶段一产出的需求 MD、第一部分引用的编码规范（`sugon_web/case_specs/prompts/test_case_codegen_prompt.md`）**和断言规范（`sugon_web/case_specs/prompts/assertion_guidelines.md`）以及对应前端工程代码**，按以下检查项逐项检查，发现遗漏或偏差直接修改，循环直至完全对齐。

> **★ 检查优先级（先查、最严格查这四项——它们最决定阶段三返工次数，是"快速写完却在阶段三反复返工"的高发根因）**：① **定位"有据性"**（见二.3：每个非平凡定位都要有据，找不到源码就 recon，绝不凭印象猜）；② **前置条件可执行**（见一.6：编辑/删除/详情类依赖的资源，脚本自建或复用，不假设环境已预置）；③ **创建/提交类表单健壮性**（见二.12：原生填 + 等加载 + 提交前断言按钮 enabled）；④ **导航真正到达目标列表页**（不是点子菜单后停在服务概览页）。这四项查到位，阶段三返工最少；其余为常规规范项，逐条过即可。

**一、需求对齐检查**

1. **步骤完整性**
   - 规则：脚本覆盖需求 MD 中的所有测试步骤，无遗漏、无多余，步骤顺序与需求一致。

2. **边界条件覆盖**
   - 规则：脚本实现需求 MD 中定义的所有边界条件测试代码。

3. **断言完整性**
   - 规则：脚本实现需求 MD 中定义的所有断言点，P0 流程阻塞点（操作成功+存在性/状态收敛）、P1 末态字段（字段值回读）、P2 业务逻辑（SSH 后端验证）三层覆盖完整无遗漏；P3 导航/操作步骤无显式断言。

4. **测试数据对齐**
   - 规则：资源名称使用 `random_data()`；批量创建必须用 fixture 的 `count` 参数，禁止手动循环创建（如 `for i in range`）；测试数据准备与前置条件一致。
   - 示例：vm fixture 用 `{"basic": {"count": N}}` 格式，其他 fixture 用 `{"count": N}` 格式。
   - 自检：`Grep` 搜索 `@pytest.mark.parametrize("vm"`，确认未出现手动循环创建。

5. **清理策略对齐**
   - 规则：脚本实现需求 MD 中定义的所有清理步骤，覆盖所有创建的资源；**清理顺序必须严格依据测试需求 CSV 的"测试数据清理顺序"字段（经 MD 转写）执行，不得自行调整**。
   - 核心原则：清理由 fixture 负责回收，测试方法只负责测试——资源已由现有 fixture teardown 完整覆盖则直接复用，现有 teardown 无法覆盖则参照 yield-based cleanup fixture 模式补充。
   - 共享数据：MD 中标注"共享同一套测试数据"的场景，必须置于同一测试类中通过 class-scoped fixture 共享资源，待该批次所有用例执行完成后统一清理。

6. **前置条件可执行性（self-provision / 复用·治"编辑/删除/详情类用例无对象可操作"）**
   - 规则：需求 MD 里"前置条件：已预置/已存在 XX 资源（如 policy1）"**不得假设自动化环境已有**——必须由 fixture/setup **自己保证存在：先查、不存在则创建（`exists()? else create`）**；若该数据 CSV 标注"可复用=是"且依赖前序新建场景，则**复用前序场景 class-scoped 共享的资源、严禁在前一场景就删除**。**严禁只 `search(预置名)` 就假定它在**（实测事故：编辑用例只搜不建、新建场景又随机命名用完即删 → 编辑无对象、连撞 15 次冻结）。
   - 自检：凡用例步骤含"对已存在的 XX 执行编辑/删除/查看详情"，确认脚本里有"保证该 XX 存在"的代码（自建或复用前序场景资源），而非搜到就直接用。

7. **用例独立性（解耦）（适用所有模块，详见 `test_case_codegen_prompt.md` §5.3）**
   - 规则：每个 `test_` 方法须可单独/任意顺序/重复运行且结果一致——资源由 fixture 自给、名称用 `random_data()`、用例内自导航、用例内自清理并复原共享资源状态、不依赖其它用例的残留数据或执行顺序；节点数/存储/架构等环境前提用 `skip_*` 装饰器声明，而非靠前序用例顺带准备。
   - 自检：设想"只 `-k` 单跑这一个用例 / 打乱顺序跑"是否仍通过；跨用例复用数据只允许同测试类 class-scoped fixture 共享，禁止靠执行顺序传递残留数据。

8. **显式字段 → Page Object 参数 反向映射核对（治"需求写了、代码没真正配上"，适用所有模块）**
   - 规则：以需求 MD 中"创建/编辑/配置"步骤**显式列出的每个字段**为源，逐项核对"MD 字段 → Page Object 调用参数 → 该参数实际取值"三者一致；并确认该字段在 Page Object 内**确有 fail-loud 的填写/选择实现**（不是仅有形参却被 `if count()>0` 静默跳过，也不是用内部默认值替代 MD 显式值）。实现层细则见「二、编码规范检查」第 13 项。
   - 自检：MD 里显式写了、但在对应 `*_create / *_edit / *_config` 调用参数里找不到，或在 Page Object 里被静默放过/用默认值替代的字段，即为"未落地"，必须补齐。
   - **闸门：存在任一显式字段未落地 → 不得进入阶段三。**

**二、编码规范检查**

1. **fixture 复用**
   - 规则：优先复用现有 fixture，禁止自行封装功能重复的 fixture；实现测试步骤或前置条件前已阅读 `fixtures_index.md` 确认参数支持情况。

2. **页面对象规范**
   - 规则：页面对象只封装页面交互与业务动作，不组织测试步骤；新增业务方法必须有 docstring（至少含用途、参数含义、可选值或默认值）；已有公共能力能覆盖时禁止新增同义方法；测试 class 体内禁止有 Helper 方法（纯函数放模块级或 `_xxx_helpers.py`）。
   - 自检：`Grep` 搜索 class 体内是否有 `_` 前缀的辅助函数。

3. **定位规范**
   - 规则：测试层严禁直接使用 `locator()` / `expect()` / XPath，所有页面交互必须通过 Page Object 封装方法调用；定位优先 `get_by_role()` / `get_by_text(exact=True)` / `get_by_placeholder()`，并限定在 dialog / tab / 表格 / 行范围内，`first()` / `nth()` 仅作兜底。
   - **运行时侦察(按需,无法仅凭前端代码确定唯一定位时)**：当某交互的定位**无法仅凭前端工程代码确定唯一、稳定的选择器**（典型：操作项默认隐藏需 JS 触发、同名菜单多个需 nth、复杂表单组件内部多 input、自定义组件如 cl-table/SugonDeleteDialog），**应先用运行时侦察脚本(黑盒,先 `--help`)** 从真实渲染态枚举候选元素再写定位，不要凭空构造或仅靠静态代码猜测：
     `python .claude/skills/test-script-dev/scripts/recon_page.py --service "<服务名>" [--submenu "<子菜单>"] [--grep "<关键词>"]`
     侦察脚本**只读不改**(复用项目登录态/`goto_service`、整页截图、浏览器端单次枚举真实渲染态元素：按钮/链接/输入/radio·checkbox/tab/列头/自研组件 cl-*·cloud-*，并报**弹窗·抽屉状态**与每个元素的**真实文案·role·class·是否 DISABLED**)，只用于发现定位，定位写回 Page Object。简单用例能直接确定定位的**不必触发**，避免拖慢。
   - **★ 定位"有据性"自检（最决定阶段三返工，务必逐个非平凡定位过）**：每个非平凡定位都必须**有据**——来自【同模块既有范式 / 前端代码取证（文件:行） / `recon_page.py` 运行态侦察】三者之一；**凡按 `module_index.yaml` 策略在模块目录里没搜到、或拿不准是哪个前端组件承载本功能时，直接 recon 据真实渲染态写，绝不凭印象/文件名猜**（详见 `frontend_to_script_guide.md`）。命中"须运行态确认"的元素（teleport 弹窗/下拉、动态/i18n 文案、自研组件 role、运行态 disabled、可疑类名）必须先复用范式或 recon 确认。
   - **★ 侦察结论卡优先，禁止"反悔猜"**：本阶段「页面侦察结论卡」一旦确认了某元素的**存在性与精确定位**，编码**必须直接采用该定位**；严禁编码时凭怀疑改用更宽松的猜测定位（例如把已确认的 `HTTP(S)方法` 放宽成 `HTTP` 致误匹配相邻字段），也严禁把它降级成"找不到就跳过"。若编码时怀疑结论卡有误，**必须重新 `recon` 取证并更新结论卡**，而不是一边留着结论卡、一边在代码里写一个与之矛盾的脆弱定位（实测事故：结论卡已确认字段存在且给出精确定位，代码却用宽松定位 + 静默跳过，字段没真正配上）。
   - **★ 导航必须真正到达目标列表页**：涉及"进入某模块/列表页"的，要确认走的是**真实 hash 路由、真到了目标列表页**（而非点子菜单后停在服务概览页如 `#/vpc-overview`）；拿不准就用 `recon_page.py --probe-click`/侦察确认当前 URL，别假设"点了子菜单就到了"（实测事故：停在概览页搜不到目标、连撞 15 次冻结）。
   - 自检：`Grep` 搜索 `\.locator\(` 或 `from.*playwright import expect`，确认测试层未出现底层 API 调用。

4. **等待规范**
   - 规则：禁止使用固定等待；进入页面或表单提交后先 `wait_for_page_ready()` 或 `wait_for_load_state('networkidle')`，状态收敛优先 `wait_for_operation_complete()` / `wait_for_source_complete()`。
   - 数据来源：CSV 已明确等待时长的，作为收敛等待方法（如 `wait_for_operation_complete()` / `wait_for_source_complete()` / `assert_status()`）的 `timeout` 参数传入，禁止转为固定等待；CSV 未明确的用框架方法默认值。

5. **断言分层合规**
   - 规则：断言按 P0/P1/P2/P3 分层，符合 `assertion_guidelines.md` 决策树判定；P3 导航/操作步骤无显式断言；P0 只断言操作成功+存在性，不混杂字段值；P0 与 P1 不重复断言同一内容。

6. **断言规范**
   - 规则：状态变更类断言按 P0→P1 顺序——P0 先断言操作成功（如 `assert_popup_success()`）和资源存在性/状态收敛（如 `assert_list_contain()` / `assert_status()` / `assert_deleted()`），P1 再集中断言末态字段值（详情页/行数据回读）；状态变更必须先等待收敛（`wait_for_operation_complete()` / `wait_for_source_complete()`）再断言；SSH 命令必须断言返回值（`rc`、`stdout`），禁止用 `` `|| true` `` 掩盖错误；每步操作后必须紧跟断言，禁止仅用 `logger.info()` 记录而不断言。
   - 新增方法：先搜索 `sugon_web/assertions/` 已有方法优先复用，无可复用时按 `assert_<对象>_<行为>` 命名、参数带默认值、写完整 docstring。

7. **用例结构规范**
   - 规则：用例结构为准备数据 → 执行操作 → 校验结果，步骤用 `with allure_step_log()`；测试主流程禁止用 `try/finally` 或 `try/except` 包裹核心步骤。
   - 自检：`Grep` 搜索 `try:` + `finally:`，确认测试方法体内未出现 `try/finally`。

8. **数量对齐**
   - 规则：CSV 场景数、MD 场景数、测试脚本中 `def test_` 方法数三者必须一致；CSV 含多个仅参数不同的高度重复需求（如同类创建场景）时，必须用 `@pytest.mark.parametrize` 参数化实现，严禁把多个需求合并进同一个测试方法、也不写多个高重复率的方法。
   - 自检：`Grep` 搜索正则 `def test_\w+` 数量，确认测试方法数与 CSV 行数一致。

9. **Allure 标题规范**
   - 规则：`@allure.title` 沿用同模块现有风格命名，格式为 `资源名-功能点`；数据驱动用例保留参数化占位风格。

10. **公共操作复用**
    - 规则：优先使用 `base.py` 已封装的公共操作元素（如 `btn_create`、`search`、`click_action` 等），禁止自行构造等价 locator 重复实现。

11. **代码侵入检查**
    - 规则：脚本仅可新增/修改测试文件（`test_*.py`）和必要的页面对象文件；禁止修改 `sugon_web/common/`、`sugon_web/refrence/` 下任何文件，也禁止修改阶段一产出的 MD 需求文档。

12. **创建/提交类表单健壮性（治"卡在创建第一步、提交按钮一直 disabled、反复超时"）**
    - 规则：含**多必填 + 异步加载（`v-loading`）+ 校验**的创建/编辑表单，按 `frontend_to_script_guide.md` §七 配方写：进表单先等 `el-loading-mask` 消失、原生 `fill`/点选项**触发真实事件链**（不只设值）、按字段依赖顺序填、**提交前断言提交按钮 `enabled`（仍 disabled 就读 `.el-form-item.is-error` 定位漏填项，1~2 次定位别盲改）**；修改类表单 `fill` 前先 `clear()` 清空旧值。
    - 禁止：用 `page.evaluate` 合成 input/change 事件盲填、JS 点 `.el-select-dropdown__item`、`evaluate` 内 `setTimeout`（`precheck.py` 会拦，也是"提交不了、反复超时"的高发根因）。

13. **显式字段强制落地，禁止"静默跳过"或默认值替代（治"需求写了、代码没真正配上"，适用所有模块）**
    - 规则：需求 MD 在创建/编辑/配置步骤里**显式列出取值**的字段（任何模块通用：名称、协议、端口、规格、副本数、调度算法、各类开关及其子项、URL、策略等），其对应的 Page Object 方法**必须有参数承载、且必须真正填/选成功**。
    - **为什么必须 fail-loud（弱模型务必理解，别凭"求脚本不报错"的本能行事）**：静默跳过看似"让脚本不报错、能跑完"，实则字段根本没配上，而用例很可能因后续断言没覆盖该字段而**照样判 PASS**——这叫"假绿 / 测试裸奔"，比当场报错**危险得多**：它给出虚假的通过信号、让真问题甩锅成"产品缺陷"、还会掩盖前端改版导致的定位失效。所以"找不到必填字段"= 真有问题，就该**当场失败、交给阶段三去定位修**；若你是怀疑字段不存在（如条件渲染、选了某项才出现），按下文「★ 侦察结论卡优先」重新 `recon` 确认，**绝不是写个防御式跳过把它吞掉**。
    - **禁止 best-effort 静默跳过**：严禁用 `if locator.count() > 0: 填; 否则什么都不做` 这类写法承载"显式必填字段"——元素找不到必须**直接报错（fail-loud）暴露问题**，而不是悄悄放过、把字段留在默认/空值（实测事故：某资源池创建的"健康检查方法"字段用了"找不到就跳过"写法 + 宽松定位，字段没真正配上 → 后端被判不健康 → curl 返回 502，却被阶段三误判为产品缺陷）。
      - **正确写法（照此做）**：直接写结论卡里已确认的定位并调用 `.fill()` / `.click()` / 点选项，**不要包 `if ...count() > 0` 守卫**——Playwright 在元素找不到时会自动超时抛错，这就是天然的 fail-loud，问题会当场暴露而不是被吞掉。
      - best-effort（`if count()>0` 才操作）**只允许用于确实可选 / 纯探测**的操作（如"有则关掉残留弹窗"），**绝不能用于需求显式要求的必填字段**。
    - **禁止默认值替代**：不得用 Page Object 内部默认值替代 MD 显式给出的值（默认值"看起来合理" ≠ 符合需求）。
    - **缺参数就扩展方法**：若 Page Object 方法签名缺少承载该字段的参数，按规范**新增带默认值的可选参数**承载它，严禁在测试层用 `evaluate` / 拼 URL / 直接操作 DOM 绕过。

**循环规则：** 按上述各项检查规范逐项检查 → 发现不符合 → 修改脚本 → 重新检查 → 直至全部符合。
- **遵循通用循环约定，严禁停下来询问用户**，因为循环中断会浪费已执行的进度，所有检查发现的问题自主修改
- 直至全部检查项均通过，方可进入下一阶段

**三、静态门禁闸门（对齐检查全部通过后、进入阶段三前必跑）**

> 上述对齐检查是 AI"自证式"逐项检查；为兜底"可机器判定"的机械违规（防止它们被带到阶段三长时执行才暴露），在对齐检查全部通过后，再跑一次静态门禁脚本（黑盒，先 `--help`），形成 validator→fix→repeat 反馈环：

```
python .claude/skills/test-script-dev/scripts/precheck.py <本次测试文件或目录> --expected-tests <CSV/MD 场景数>
```

- 门禁客观检出：测试层 `.locator(`/`expect`/XPath、`wait_for_timeout`/`time.sleep` 固定等待、测试方法体 `try/except|finally` 包裹、SSH `|| true`、`def test_` 数 ≠ 场景数、未在 pytest.ini 登记的 marker、触碰禁区文件。
- **★ 目标文件已含其它用例时的 `--expected-tests` 口径（防"为过门禁删已有用例"）**：`precheck` 统计的是**整个文件的 `def test_` 用例总数**。若本次是往一个**已存在、已含其它用例**的文件里追加，则 `--expected-tests` 必须传**文件最终用例总数 =（写入前已有用例数）+（本次 CSV 新增场景数）**，不是只传本次场景数。**若门禁报"数量不符"，先判断是不是因为文件本来就有其它用例——是则把 `--expected-tests` 改成正确总数重跑；绝对禁止靠删除已有用例来让门禁数字相等**（删已有用例是严重事故，见上文"已有测试文件保护"）。
- **退出码非 0 → 必须先修复违规再重跑门禁，直至通过方可进入阶段三**。
- **门禁是兜底，不是替代**：断言分层是否合理、是否对齐需求语义，仍由上面"一、需求对齐检查 / 二、编码规范检查"负责；门禁通过不代表语义正确。

## 阶段二完成时立即输出（必须，不得延迟到 SKILL 总收尾）

**触发时机**：本阶段达到完成标志或被迫中断时,立即在对话框输出以下内容（不得跳过、不得合并到下一阶段）。

> **同步持久化（强制）**：以下内容在对话框输出的同时，必须**追加写入运行报告文件** `skill_runs/test-script-dev/test-script-dev_{任务标识}_{YYYYMMDD_HHMM}.md`（任务标识 = 本次任务的需求 MD 英文文件名去 `.md`，与阶段一一致，不得改用脚本名）。追加前先按任务标识 glob 取最新的该运行报告文件并在其末尾追加，**严禁新建第二个文件**；仅当以本阶段作为执行入口、glob 确实找不到任何文件时，才创建并写入文件头（任务标识、运行开始时间、来源信息）。追加内容最前面须记录本阶段完成时间，格式：`## 阶段二：脚本编写与对齐　完成时间：YYYY-MM-DD HH:MM:SS`。 **【完成时间必须精确到秒·取值铁律】**：上面的 `YYYY-MM-DD HH:MM:SS` 是格式占位符、不是要照抄的字面值——**必须先在 git-bash 运行 `date "+%Y-%m-%d %H:%M:%S"` 取真实系统时间，把它的输出（形如 `2026-06-18 11:19:43`）原样填进标题**；严禁凭记忆/猜测手写，严禁只写日期（如 `2026-06-18`）、严禁省略时:分:秒，也严禁只写时分秒而缺日期——**年月日 与 时:分:秒 必须同时齐全**。（阶段完成后跑 `report_check.py` 会机械校验该时间，缺秒或缺日期一律判违规、必须改写后重跑。）**确认本阶段完成块已写入该运行报告文件后，方可进入下一阶段。**
>
> **落笔前就近重读 + 写后校验（强制）**：生成本阶段输出前，先重新 `Read` 本文件「## 阶段二完成时立即输出」整节，按其确切要求逐项写全（状态标题 + 完成清单表 + 页面侦察结论卡「新建/改动 Page Object 或含复杂交互时必出，简单用例写一行"无需侦察"即可」 + **对齐自检清单表**「必出·标准 Markdown 表格，表头 `| 检查项 | 结果(通过/不通过) | 说明 |`，逐项列「第二部分」一(需求对齐检查)+二(编码规范检查)全部检查项及其结果」）；写完后运行 `python .claude/skills/test-script-dev/scripts/report_check.py <本次运行报告 md 路径> --phase 2`，退出码非 0 则按提示补写后重跑，通过方可进入下一阶段。

1. **状态标题**
   - 正常完成 → `阶段二任务已完成`
   - 被迫中断 → `阶段二任务被迫中断：{主要原因}`。如有深层根因则补充，无则省略。

2. **完成清单**

| 测试脚本路径 | 脚本名称 | 测试方法列表 | 覆盖场景数 | 对齐检查结果 |
|---|---|---|---|---|
| `sugon_web/testcase/xxx/test_xxx.py` | `test_xxx.py` | `test_xxx1`, `test_xxx2` | N | 通过/不通过 |

3. **页面侦察结论卡（架构新增·打通 phase2→phase3 知识断层，2026-06-17）**

> **为什么必须写**：阶段三是 fresh-context 子智能体，看不见你本阶段的侦察过程。把你"看真实页面/读前端代码"得到的关键结论固化成下表，阶段三修复时会强制先读它、命中即直接用，**不再从零重新 recon/重新猜**——这是减少阶段三反复试错、缩短耗时的核心。
>
> **何时必出（条件触发）**：本次**新建/改动了 `sugon_web/pages/` 下的 Page Object**，或交互含**多步向导 / 自定义组件(cl-*) / 多 dropdown / 动态渲染 / 同名元素需 nth** 时，**本表必出**（应与 `recon_page.py` 侦察产物一致）。若本次为极简用例、未新建 Page Object、定位全部可由前端代码直接确定，可在表内写一行"本次无需侦察：定位均由前端代码直接确定"。
> **禁止凭空填写（防幻觉·硬约束）**：表里**每一行都必须有真实证据来源**——填进"证据来源"列：`recon_page.py` 的侦察产物（`skill_runs/recon/` 下的侦察截图路径，**或它控制台枚举出的真实元素清单**——增强枚举会直接给出每个元素的 role/class/真实文案/是否 DISABLED 及弹窗/抽屉状态，这些就是写 class/role 类定位的第一手证据），或被测前端代码的 `文件:行号`。**严禁凭记忆/猜测/想当然写定位或文案**；没有证据的元素**宁可不写**也不要编造（写错的定位会直接误导阶段三，比不写更糟）。本卡只记录"你已经亲眼/亲手核实过的"结论。
> **同步持久化（强制）**：本表与上面的完成清单一并双写进运行报告（`## 阶段二…完成时间` 块内）。

| 元素/动作 | 真实定位(get_by_*/scope) | 已确认文案 | 所在容器(dialog/tab/表格/行) | 向导步数/下拉选项 | 已知交互坑 | 证据来源(recon截图/前端代码行) |
|---|---|---|---|---|---|---|
| 如：新建资源池按钮 | `get_by_role("button", name="新建")` 限定在 pool tab | "新建" | 资源池 tab 头部 | — | 同名"新建"有 2 个，需 tab 内限定 | `skill_runs/recon/xxx.png` / `lb/PoolList.vue:42` |
| 如：用途下拉 | `get_by_text("云服务器实例", exact=True)` | "云服务器实例" | 新建规格 dialog | 选项：云服务器实例/裸金属… | el-select 点选后**不自动关闭**，需点空白处收起再操作下一个下拉 | recon 截图 |
