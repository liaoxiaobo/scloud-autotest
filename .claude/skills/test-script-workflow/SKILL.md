---
name: test-script-workflow
description: |
  UI 自动化测试脚本开发的完整工作流 skill。覆盖测试需求CSV文件转测试需求MD文档 → MD文档质量校验 → 编写自动化测试脚本 → 脚本与需求对齐检查 → 执行用例并修复错误 → 分析执行日志 → 核查测试需求覆盖度 → 多轮稳定性验证 → 输出完成报告的完整闭环。本skill是长时间自主运行任务，全程无需人工确认。支持指定入口来执行本skill的部分阶段。
model: opus
allowed-tools: [Read, Edit, Write, Bash, Grep, Glob, Agent]
user-invocable: true
---

# UI 自动化测试脚本开发 SKILL

## 长时间自主任务声明

本 SKILL 执行期间 **AI 全程自主运行**，严禁停下来询问用户，因为本任务设计为长时间自主运行，中途等待用户响应会中断自动化流程。所有修复/检查决策自主完成，仅在无法自主解决的阻塞性问题时允许暂停请求输入。任务从需求转换到稳定性验证可能持续数十分钟至数小时。

> **设计哲学**：给足修复机会，但绝不死磕；检测无意义循环，而非掐表计时。

---

## 运行报告持久化与恢复协议

为对抗上下文压缩导致前序阶段输出丢失，各阶段"立即输出"须在对话框输出的同时，双写到运行报告文件 `skill_runs/test-script-workflow/test-script-workflow_{任务标识}_{YYYYMMDD_HHMM}.md`（具体写入要求见各 phase 文件末尾"同步持久化"段）。全局规则：

- **任务标识与单文件**：任务标识 = 测试脚本基名去 `test_` 与 `.py`；阶段一/二脚本名未定时先用需求 MD 英文文件名（去 `.md`），后续不改名。一次运行只用一个文件（放 `skill_runs/test-script-workflow/`，严禁写入 `logs/`），五阶段及回退轨迹均追加进去，严禁覆盖前序内容。
- **双写不替代**：严禁用写文件替代对话框输出，也严禁用对话框输出替代写文件。
- **压缩后续跑**：每阶段开始前先按 glob `skill_runs/test-script-workflow/test-script-workflow_{任务标识}_*.md` 取最新文件 `Read`，据此判断已完成到哪、从哪重入；任务标识从磁盘产物（需求 MD / `test_*.py`）推导，不靠对话记忆。
- **总收尾从文件聚合**：全部完成后的总收尾各表格须先 `Read` 报告文件后聚合，不依赖对话记忆。

---

## 五个阶段的执行编排

**本文件（SKILL.md）为总控文件**，只定义工作流编排、阶段顺序、文件读取关系和全局约束。**各阶段的具体执行指令、提示词、要读取的关联文件、检查清单详见 `references/` 目录下的对应文件及其引用的关联文件**，执行各阶段前必须先读取对应 reference 获取完整指令。

执行以下各个阶段任务的过程中，必须先使用 Read 工具完整读取下文提到的该阶段"读取文件"字段中 `references/` 目录下的 phase 文件，以获取该阶段的完整执行指令。应严格按照顺序读取 phase 文件及其列出的关联文件。严禁在未读取 phase 文件及其列出的关联文件的情况下开始执行阶段任务，因为各阶段指令分散在 phase 文件及其引用的关联文件中，跳过阅读会导致执行偏差。

### 阶段一：需求转换与校验
将 CSV 测试需求文件转换为结构化 MD 需求文档，并进行质量校验。

**读取文件：** `references/phase_01_requirements.md`（第一部分+第二部分），以及其引用的 `sugon_web/case_specs/templates/base_case.md`、`sugon_web/case_specs/fixtures_index.md`、用户提供的 CSV 文件
**文件内容：** CSV→MD 转换规范与 MD 质量校验
**前置条件：** 用户提供 CSV 测试需求文件
**执行顺序：** 先执行第一部分（CSV→MD），在同一文件中继续执行第二部分（MD 校验）
**完成标志：** CSV 已转换为规范的 MD 需求文档（若用户直接提供，标记为跳过），且 MD 文档已校验通过，无歧义无错误
**产出物：** 校验通过的 MD 需求文档
**异常处理：** 无

### 阶段二：脚本编写与对齐
基于 MD 需求编写 Playwright + Pytest 自动化测试脚本，并与需求进行对齐检查。

**读取文件：** `references/phase_02_coding.md`（第一部分+第二部分），以及其引用的 `sugon_web/case_specs/prompts/test_case_codegen_prompt.md`、`sugon_web/case_specs/fixtures_index.md`、`sugon_web/case_specs/prompts/assertion_guidelines.md`、阶段一产出的测试需求 MD 文件、`sugon_web/refrence/module_index.yaml`、被测模块对应的前端工程代码
**文件内容：** 编码规范、断言规范、用例骨架、需求对齐与编码规范检查
**前置条件：** 阶段一完成
**执行顺序：** 先执行第一部分（编写脚本），在同一文件中继续执行第二部分（对齐检查）
**完成标志：** 脚本与需求完全对齐，各项检查全部通过
**产出物：** 对齐需求后的测试脚本
**异常处理：** 若无法定位被测模块对应的前端工程代码（按 `phase_02_coding.md` 中"匹配失败"的处理方式执行）

### 阶段三：执行用例与修复
执行测试用例，修复错误直至所有用例达到可接受状态。

**读取文件：** `references/phase_03_execution.md`，以及其引用的 `references/phase_02_coding.md`、阶段一产出的测试需求 MD 文件、`sugon_web/refrence/` 下被测模块的前端工程代码
**文件内容：** pytest 执行与四步法修复：日志规范、Reproduce→Isolate→Diagnose→Fix、检查清单
**前置条件：** 阶段二完成（或用户提供现有脚本及对应 MD 文档）
**完成标志：** 所有用例达到可接受状态（用例问题已修复、产品缺陷已标记、环境问题已跳过）
**产出物：** 修复后的脚本 + 检查清单 + 日志文件
**异常处理：** 用例失败在阶段内部按四步法（Reproduce→Isolate→Diagnose→Fix）处理，无需跨阶段跳转。

### 阶段四：日志分析与核查
分析执行日志，核查测试需求覆盖度。

**读取文件：** `references/phase_04_log_analysis.md`，以及其引用的阶段一产出的测试需求 MD 文件
**文件内容：** 日志分析与需求覆盖度核查：重要/非重要问题分级、完成标志
**前置条件：** 阶段三完成
**完成标志：** 日志分析完成，每个步骤与需求一致
**产出物：** 日志分析与核查报告
**异常处理：** 若日志分析发现重要需求未满足 → **返回阶段三**修复脚本后重新分析；若仅非重要细节问题 → 直接进入阶段五

### 阶段五：稳定性验证
多次执行验证脚本稳定性（至少 3 次）。

**读取文件：** `references/phase_05_stability.md`
**文件内容：** 稳定性验证：至少 3 次运行、判定规则（环境/产品/用例）、经验沉淀
**前置条件：** 阶段四完成
**完成标志：** 连续三次运行均无未修复的用例问题（环境问题跳过、产品缺陷标记不计入）；若已标记产品缺陷，阶段五正常结束并提示提交缺陷
**产出物：** 稳定性验证结果
**异常处理：** 用例失败在阶段内部按稳定性判定规则处理，无需跨阶段跳转。

---

## 工具调用约束

修改测试脚本时，若 `Edit` 因 `old_string` 不唯一失败，严禁中断流程等待人工确认。按以下优先级自动恢复：

1. **扩大匹配范围**：立即 `Read` 文件，增加 surrounding 函数体或相邻注释使 `old_string` 唯一
2. **使用 replace_all**：确认所有匹配处需相同修改时，改用 `replace_all=true`
3. **替代方案**：上述方法均不可行时，改用 `Write` 重写整个文件

`Edit` 和 `replace_all` 均失败时，必须使用 `Write` 作为最终降级方案；仅当 `Write` 也失败时，记录错误并继续下一步。严禁弹出确认对话框。

---

## 完成后输出（按以下顺序，与各阶段的"立即输出"并行执行，不二选一）

**重要**：每个阶段执行完毕时**必须立即**输出该 phase reference 文件末尾定义的"阶段完成时立即输出"内容（含状态标题、完成清单、阶段特有产出如阶段五的"修复经验沉淀"），**并同步追加写入运行报告文件**（见上文《运行报告持久化与恢复协议》）；所有阶段全部完成后，再额外输出本节定义的总收尾（阶段产出表 + 改动文件 + 总结报告）。两者并行，**严禁用本节内容替代任一阶段的立即输出**。

全部阶段执行完毕并通过完成判定后，按以下顺序在对话框输出总结，并将总收尾追加写入运行报告文件末尾。表格内容须按《运行报告持久化与恢复协议》"总收尾从文件聚合"规则先 `Read` 报告文件后聚合生成，不依赖对话记忆。

### 1. 阶段产出表

按以下 Markdown 表格格式输出，每个阶段占一行，多文件分多行展示：

| 执行阶段 | 新创建/编辑的文件 | 文件用途 | 是否检查/测试通过 |
|---|---|---|---|
| 阶段一：需求转换与校验 | `sugon_web/case_specs/xxx/xxx.md` | 结构化需求文档 | 是 |
| 阶段二：脚本编写与对齐 | `sugon_web/testcase/xxx/test_xxx.py` | 自动化测试脚本 | 是 |
| 阶段三：执行用例与修复 | `sugon_web/pages/xxx/xxx.py` | 修复页面对象/测试脚本 | 是/部分/否 |
| 阶段四：日志分析与核查 | — | — | 是/跳过 |
| 阶段五：稳定性验证 | — | — | 是/跳过 |

### 2. 改动文件

按以下 Markdown 表格格式输出，每个文件占一行：

| 相对路径 | 绝对路径 | 新增行数 | 删除行数 |
|---|---|---|---|
| `sugon_web/testcase/xxx/test_xxx.py` | `/path/to/sugon_web/testcase/xxx/test_xxx.py` | +100 | -10 |

### 3. 总结报告
严格按照 `sugon_web/case_specs/prompts/test_case_codegen_prompt.md` 的《输出报告格式要求》输出。
