# 阶段五：稳定性验证（连续三次通过）


```
请对阶段二生成的所有测试文件各运行至少三次，不得遗漏任何文件，来检查测试脚本的稳定性。

**本阶段全程自主运行，严禁停下来询问用户。循环中断会浪费已执行的进度。**

验证过程中严禁修改阶段一产出的 MD 需求文档，除非确认 MD 本身存在转换错误。

**每次运行必须遵守阶段三的执行前置要求**：执行 pytest 前通过 `Bash` 定义 `LOG_FILE` 变量并传入 `--log-file=$LOG_FILE --log-file-level=DEBUG`，严禁省略 `--log-file`。日志命名规则同阶段三（按测试 py 文件名命名，如 `sugon_web/logs/test_xxx.log`）。

若用例执行失败，按阶段三的四步法分析根因并处理：环境问题跳过、产品缺陷标记、用例问题修复。修复纪律与判定规则同阶段三。

直至连续三次运行均无未修复的用例问题（环境问题跳过、产品缺陷标记不计入），则视为完成阶段五（即整个工作流任务完成）。
```

**稳定性判定规则：**

| 失败根因 | 处理方式 |
|---|---|
| 环境问题 | 忽略，继续运行 |
| 产品缺陷 | 🚨 醒目提示"**发现产品缺陷，请提交缺陷**"，附复现步骤+关键日志+截图 |
| 用例问题 | 直接修复代码，修复后重新运行验证 |

连续三次运行均无未修复的用例问题（环境问题跳过、产品缺陷标记不计入） → **阶段五完成，即整个工作流任务完成** ✅

**输入/输出示例：**

输入（某次运行结果）：
```
第1轮运行：PASSED（0 失败）
第2轮运行：FAILED（1 失败）
  - test_lb_v2_create.py::TestLBV2Create::test_lb_v2_tcp_scenario
  - 错误：AssertionError: assert '运行中' == '已停止'
  - 实际：后端返回 '已停止'，但预期是 '运行中'
第3轮运行：PASSED（0 失败）
```

输出（判定与处理）：
```
第2轮失败判定：
- 根因类型：用例问题（断言逻辑错误，预期值与实际状态不一致）
- 处理：检查后端真实状态，发现LB实例创建后默认状态为"已停止"，需等待启动完成。修复断言为 assert_status(['lb_name'], '运行中')。
- 第4轮运行：PASSED
- 第5轮运行：PASSED
- 连续三次无未修复用例问题 → 阶段五完成 ✅
```

**循环规则：** 按判定规则处理每次失败 → 用例问题修复代码后重跑 → 环境问题记录后不计入统计继续下一轮 → 产品缺陷标记后继续下一轮 → 直至满足上述完成条件。

**阶段完成输出**

本阶段结束（正常完成或被迫中断）时，在对话框中输出：

1. **状态标题**
   - 正常完成 → `阶段五任务已完成`
   - 被迫中断 → `阶段五任务被迫中断：{主要原因}`。如有深层根因则补充，无则省略。

2. **完成清单**

| 验证文件 | 运行轮次 | 通过轮次 | 连续通过次数 | 验证结论 |
|---|---|---|---|---|
| `test_xxx.py` | N | N | N | 完成/未完成 |

**问题总结与经验沉淀（阶段五完成后必须输出）：**

### 分类汇总

| 序号 | 阶段 | 问题简述 | 根因类型 | 修复方案 | 状态 |
|------|------|----------|----------|----------|------|

按根因类型分类统计：
- **用例问题**：共 N 处，均为脚本编写或定位方式问题
- **环境问题**：共 N 处，均为环境波动或网络超时
- **产品缺陷**：共 N 处，已醒目提示提交缺陷

### 修复经验沉淀

针对本次高频出现的问题，用 1~2 句话沉淀可复用的修复经验，例如：
- 该模块列表页加载较慢，后续同类用例统一增加 `wait_for_load_state("networkidle")`
- 该功能删除操作需二次确认，后续需显式处理确认弹窗
