# 阶段五：稳定性验证

## 一、执行总纲

本阶段对每个测试文件执行稳定性验证，轮次按用例成本**自适应**（见 1.1），全部轮次执行完毕后统一判定结果。执行过程中严禁中途修复、结束或跳转其他阶段。

### 1.1 自适应轮次（按用例单次墙钟成本分级）

> **依据**：稳定性验证的价值是"用重复执行暴露偶发抖动"。对单次墙钟很长的昂贵用例（如裸金属 BMS 单次 70~100min），固定 5 轮会带来 7.5 小时级的纯验证耗时，而 5 轮相对 3 轮对"偶发抖动"的边际发现收益很低、远不及墙钟代价。故按成本分级，**不是降低标准，而是把验证强度用在性价比合理处**。

| 用例单次墙钟成本 | 稳定性轮次 | 判定"通过轮次"阈值 |
|---|---|---|
| 低成本（< 15min） | **5 轮**（维持原标准，多跑无妨，保留高置信度） | ≥ 3 轮通过 |
| 高成本（> 15min） | **3 轮** | ≥ 2 轮通过 |

- 成本依据需求 MD 中各步骤等待时长之和粗判（与阶段三 6.1 同口径）。
- **含 `pytest.skip`（产品超时）或已标记产品缺陷的用例**：不重复消耗稳定性轮次。
- 阈值随轮次同步（5 轮→≥3、3 轮→≥2），**判定决策树本身不变**（见 3.2）。

**执行注意事项**

1. **全量执行约束**：必须完整执行所选定的轮次数，无论中间轮次结果如何，严禁提前终止。
2. **禁止中途修复**：所选轮次执行完毕前，发现用例问题也不得返回阶段三修复，须等全部轮次执行完毕后统一处理。
3. **MD 文档保护**：验证过程中严禁修改阶段一产出的 MD 需求文档，除非确认 MD 本身存在转换错误。
4. **日志规范**：每次运行 pytest 前，通过 `Bash` 定义 `LOG_FILE` 变量（路径在项目根目录的原生日志目录 `logs/` 下，命名按测试 py 文件名），并传入 `--log-file=$LOG_FILE --log-file-level=DEBUG`。严禁执行不带 `--log-file` 的 pytest 命令。
5. **失败处理**：每轮执行后若存在失败用例，按阶段三四步法分析根因并记录，但**仅记录、不修复、不阻断后续轮次**。

## 二、"环境问题"的明确定义

1. **定义**：某用例因环境波动失败且被判定为**环境问题**时，本轮 pytest 仍须完整执行所有用例（不得中途终止）。待本轮全部执行完毕后，在结果分析阶段将该用例标记为"环境问题"。
2. **核心原则**："环境问题"只能在结果分析阶段标记，严禁以任何方式修改测试脚本来绕过失败，包括但不限于：`pytest.skip()`、`@pytest.mark.skip`、`try/except` 捕获异常后既不处理也不重新抛出。
3. **流程**：pytest 完整执行本轮所有用例 → 某用例因环境波动失败 → AI 分析根因确认为环境问题 → 记录失败原因与环境波动特征 → 标记为"环境问题" → 继续执行下一轮。

## 三、执行与判定规则

### 3.1 执行规则

- 按 1.1 自适应轮次执行（低成本 5 轮 / 高成本 3 轮），每轮执行该文件的全部用例，不得遗漏。
- 每轮执行前按日志规范定义 `LOG_FILE`。
- 某轮中存在失败用例时，分析根因并记录，然后继续执行下一轮。

### 3.2 判定决策树

必须在所选轮次（低成本 5 轮 / 高成本 3 轮）全部执行完毕后，再按以下决策树判定。

**判定前说明**：
- "失败轮次中有 用例问题"指：只要任意一轮的任意一个失败用例，其根因判定为**用例问题**，即触发该分支。
- "失败轮次中有 产品缺陷"指：只要任意一轮的任意一个失败用例，其根因判定为**产品缺陷**，即触发该分支。
- "遗留问题"指阶段三因轮次耗尽或状态冻结而强制标记的问题，正常情况下不参与阶段五验证；如带入则按用例问题路径处理（返回阶段三）。
- "通过轮次"指：该轮 pytest 执行结果为 PASSED，即该轮所有用例均执行成功，无任何 FAILED 用例。
- "失败轮次"指：该轮存在根因为用例问题/产品缺陷/环境问题/遗留问题的 FAILED 用例，即该轮 pytest 执行结果为 FAILED。
- "通过轮次达标"指：按 1.1 阈值——低成本用例 ≥ 3 轮通过，高成本用例 ≥ 2 轮通过。
- 无论最终判定结果如何，均需在问题总结中如实记录所有失败轮次的根因。

```
失败轮次中有 用例问题？
  ├─ 是 → 【验证不通过】返回阶段三修复用例
  │         （按阶段三四步法修复，重新执行阶段四、阶段五。同一根因的用例问题最多修复 2 次，
  │          超过 2 次仍失败的，记录为遗留问题并结束本阶段任务）
  └─ 否 → 通过轮次达标（低成本 ≥3 / 高成本 ≥2）？
       ├─ 是 → 【验证通过】阶段五结束任务，稳定性验证通过
       └─ 否 → 失败轮次中有 产品缺陷？
                ├─ 是 → 【验证不通过】阶段五结束任务，醒目提示"发现产品缺陷，请提交缺陷"
                │         （若失败轮次中同时含环境问题，提示"环境问题及产品缺陷"）
                └─ 否 → 【验证不通过】阶段五结束任务，提示根因为环境问题
                          （已排除用例问题和产品缺陷，剩余失败轮次即为纯环境问题）
```

## 四、阶段五完成时立即输出（必须，不得延迟到 SKILL 总收尾）

**触发时机**：本阶段达到完成标志或被迫中断时,立即在对话框输出以下内容（不得跳过、不得合并到下一阶段；**4.3 节"问题总结"与 4.4 节"项目能力复用与经验沉淀"均为本阶段必出内容，不得遗漏**）。

> **同步持久化（强制）**：以下内容（含 4.3 节问题总结、4.4 节项目能力复用与经验沉淀）在对话框输出的同时，必须**追加写入运行报告文件** `skill_runs/test-script-workflow/test-script-workflow_{任务标识}_{YYYYMMDD_HHMM}.md`（任务标识 = 本次任务的需求 MD 英文文件名去 `.md`，与阶段一一致，不得改用脚本名）。追加前先按任务标识 glob 取最新的该运行报告文件并在其末尾追加，**严禁新建第二个文件**；仅当以本阶段作为执行入口、glob 确实找不到任何文件时，才创建并写入文件头（任务标识、运行开始时间、来源信息）。追加内容最前面须记录本阶段完成时间，格式：`## 阶段五：稳定性验证　完成时间：YYYY-MM-DD HH:MM:SS`。**确认本阶段完成块已写入该运行报告文件后，方可进入 SKILL 总收尾。**
>
> **落笔前就近重读（强制，对抗上下文压缩）**：阶段五经历多轮长时执行，本节确切表结构可能已被上下文压缩摘要化。**生成上述输出（尤其 4.4）之前，必须重新用 `Read` 工具完整读取本文件「## 四、阶段五完成时立即输出」整节**，严格按 4.1~4.4 的确切表头逐项输出——4.4 第一部分四张表（① Fixture ② Page Object ③ Helper ④ 断言）+ 第二部分 10 列经验沉淀表**一张都不能省、一列都不能漏**，禁止凭记忆精简或合并。
>
> **写后校验闸门（强制，机器客观校验）**：写完本阶段块后，必须运行报告锚点校验，确认 4.4 等必出区块未被漏写：
> `python .claude/skills/test-script-workflow/scripts/report_check.py <本次运行报告 md 路径> --phase 5`
> 退出码非 0（提示缺锚点）→ 必须按提示补写缺失区块并重跑校验，直至通过，方可进入 SKILL 总收尾。

### 4.1 状态标题

| 场景 | 输出 |
|:---|:---|
| 验证通过 | `阶段五任务已完成：稳定性验证通过` |
| 验证不通过，含用例问题 | `阶段五任务已结束：发现用例问题，返回阶段三修复` |
| 验证不通过，无 用例问题，含产品缺陷 | `阶段五任务已结束：发现产品缺陷，请提交缺陷` |
| 验证不通过，无 用例问题，纯环境问题 | `阶段五任务已结束：稳定性受环境影响，建议优化测试环境` |
| 被迫中断 | `阶段五任务被迫中断：{主要原因}` |

### 4.2 完成清单

| 验证文件 | 执行轮次 | 通过轮次 | 失败轮次 | 验证结论 | 后续动作 |
|---|---|---|---|---|---|
| `test_xxx.py` | 5 或 3（按成本） | N | 轮次-N | 通过/不通过 | 结束/返回阶段三 |

### 4.3 问题总结

> **范围说明**：必须汇总整个工作流（阶段一至阶段五）的全部问题。即使验证通过，也需记录失败轮次的根因。

| 轮次 | 结果 | 根因类型 | 问题简述 | 处理结果 |
|---|---|---|---|---|
| 第1轮 | PASSED | — | — | — |
| 第2轮 | FAILED | 用例问题 | 定位方式错误 | 返回阶段三修复 |
| ... | ... | ... | ... | ... |

按根因类型统计：
- **用例问题**：共 N 处
- **环境问题**：共 N 处
- **产品缺陷**：共 N 处，已提示提交缺陷
- **遗留问题**：共 N 处（正常情况下不参与阶段五验证；如带入则按用例问题路径处理）

**修复经验沉淀**　→ 见下方 4.4 独立章节（本表已升级并拆分为「能力复用」+「经验沉淀总结」两部分）。

---

## 4.4 项目能力复用与经验沉淀（独立大章节，必出）

> **目的**：把本次运行的「复用/新增了哪些项目能力」和「修复经验」结构化沉淀下来，使**其他电脑、其他会话在零上下文**情况下也能准确读懂，并据此持续完善自动化项目与本 skill。
> **填写总则（强制，对抗 AI 写得过简）**：
> - 假设读者**没有任何本次对话上下文、且在另一台电脑上**——凡涉及代码处，必须给出**文件路径 + 行号/方法签名/选择器原值→新值**，不得只写结论性的一句话。
> - 每条经验须写清「现象 → 根因 → 依据 → 方案」完整链条；关键报错须摘录**原文关键句**，不得仅转述。
> - 宁可具体啰嗦，不可笼统简略；表格单元格内容长时可用分号分点。

### 第一部分：项目已有能力复用情况

> 记录本次执行中对项目既有能力的复用与新增，便于复盘"是否重复造轮子"并指导后续复用。

**① Fixture 复用清单**

| Fixture 名称 | 来源文件/类（路径） | 作用域 | 复用/新增 | 复用或新增的具体原因 | Fixture 功能说明 |
|---|---|:---:|:---:|---|---|
| 复用示例：`vm` | `sugon_web/testcase/conftest.py` | function | 复用 | 已支持 count 批量创建，参数化即可满足，无需新增 | 批量创建弹性云服务器并自动回收 |
| 新增示例：`lb_v2` | `sugon_web/testcase/network/conftest.py` | function | 新增 | 现有 fixture 无 V2 监听器创建能力，本场景必需 | 创建 V2 监听器并自动清理 |

**② Page Object 公共方法复用清单**

| 方法名（含类） | 来源文件/类（路径） | 复用/新增 | 复用或新增的具体原因 | 方法功能说明 |
|---|---|:---:|---|---|
| 复用示例：`BasePage.click_action` | `sugon_web/common/components/actions.py` | 复用 | 通用行操作能力，兼容平铺/下拉菜单，直接满足需求 | 点击资源行的操作项 |
| 新增示例：`LbPoolPage.lb_forward_rule_create` | `sugon_web/pages/network/slb/lb_pool.py` | 新增 | base.py 无转发规则创建能力，且为 SLB 专属交互 | 在转发规则 tab 创建一条规则（条件+转发动作） |

**③ 辅助函数（Helper）复用清单**

| 方法名 | 来源文件/类（路径） | 复用/新增 | 复用或新增的具体原因 | 方法功能说明 |
|---|---|:---:|---|---|
| 复用示例：`collect_lb_http_responses` | `sugon_web/testcase/network/_slb_helpers.py` | 复用 | 已有的 curl 采集+统计函数，本场景轮询验证可直接复用 | 多次 curl 采集 LB 响应并归一化统计 |
| 新增示例：`setup_vm_http_backend` | `sugon_web/testcase/network/_slb_helpers.py` | 新增 | 多 VM 起 http 后台服务为本场景特有，无现成函数 | SSH 进 VM 起 python http.server 并轮询就绪 |

**④ 断言复用/新增清单**

> 逐条记录本次每个测试步骤的断言决策（是否需要断言、属哪个分层、复用还是新增），用于复盘断言覆盖是否合理、是否重复造轮子。

| 步骤序号 | CSV 预期结果 | 是否需要断言 | 是否断言的理由 | 断言分层（P0/P1/P2/P3，依据 `assertion_guidelines.md`） | 断言方法名 | 来源文件 | 复用/新增 | 复用或新增的具体原因 | 断言方法功能说明 |
|:---:|---|:---:|---|:---:|---|---|:---:|---|---|
| 示例：6 | 规则列表存在 rule_1 | 是 | 创建是流程阻塞点，规则未生效则后续 curl 无意义 | P0 | `assert_popup_success` | `sugon_web/assertions/base/popup.py` | 复用 | 通用成功弹窗断言，任何创建操作适用 | 断言操作成功 Toast 出现 |
| 示例：10 | curl 返回 this is vm1 | 是 | 转发正确性是本用例核心业务出口 | P2 | 测试层 `assert rc==0 and 内容匹配` | `test_slbv2_http_forward_rule.py` | 新增 | SSH 后端验证为本场景特有，无现成方法 | 校验 curl 返回码与响应内容 |
| 示例：1 | 进入负载均衡列表页 | 否 | 纯导航步骤，Page Object 内部已含等待 | P3 | — | — | — | 导航类无需显式断言 | — |

### 第二部分：修复经验沉淀总结

> 每行 = 一个被修复/标记的问题。各字段填写说明见表格下方编号列表，务必按「填写总则」写详细。

| 所属阶段 | 问题描述 | 问题具体根因 | 根因分析依据 | 涉及文件与定位 | 关联组件/适用场景标签 | 最终修复方案及原因 | 经验沉淀总结 | 复发概率·优化优先级 | 建议沉淀去向 |
|:---:|---|---|---|---|---|---|---|:---:|---|

**字段填写说明**（逐列对照填写，宁详勿简）：

1. **所属阶段**：该问题首次被发现或高发的阶段（阶段一~五）。
2. **问题描述**：失败现象 + **报错原文关键句**（如 `locator ... resolved to 0 elements`），不得只写"定位失败"这类笼统词。
3. **问题具体根因**：真正的根因，而非表象（如"Vue v-if 选值后销毁并重建了表单项 DOM"，而非"找不到元素"）。
4. **根因分析依据**：判定根因所凭的证据，标注来源——截图 / 日志 / 代码 / 后端取证（scli·SSH）。
5. **涉及文件与定位**：精确到 `文件路径:行号 方法名`，定位/断言类须写 **原值 → 新值**（如 `lb_pool.py:142 lb_forward_rule_create，.el-form-item 占位符过滤 → .form-left 区域定位`）。**这是无上下文可复现的命门，必填。**
6. **关联组件/适用场景标签**：本问题牵涉的前端组件/技术特征 + 该经验可推广到的模块/操作类型，合并为一组检索标签，便于后续按特征查同类问题。例：`Element UI 表单 + Vue v-if 动态渲染；适用：转发规则、策略配置等动态表单`、`cl-button 自定义组件；适用：BMS 等自定义组件页面`。
7. **最终修复方案及原因**：实际怎么修的 + 为什么这么修（含与其他候选相比的取舍理由，一句话带过即可，如"用 `.form-left` 区域定位替换占位符过滤：区域类名最稳定、不依赖 v-if 渲染时序，优于'选值前先存引用'方案"）。
8. **经验沉淀总结**：从本案例提炼的**可复用规律/方法论**（如"含 v-if 动态切换的表单，禁止依赖会消失的占位符做容器定位，应用稳定区域/类名"），要写成"下次遇到同类情况应怎么做"。
9. **复发概率·优化优先级**：用"复发概率（高/中/低）·优先级（1~5★）"合并表示——复发概率指该问题在其他用例再现的可能性，★越多越应尽快据此优化项目/skill。如 `高·★★★★☆`。
10. **建议沉淀去向**：该经验应回填到哪里去固化——① 回填 `phase_02_coding.md` 编码/定位规范；② 回填 `phase_03_execution.md` 速查表/分析原则；③ 提请新增/增强 fixture 或 Page Object 公共方法；④ 提请补充 `case_specs/prompts/` 下规范文档；⑤ 仅记录（个案，不具普适性）。

**填写示例（一行，体现详细度要求）**：

> 阶段三 ｜选中条件类型后 `.el-form-item` 过滤失败，报 `locator resolved to 0 elements` ｜Vue `v-if` 在选值后销毁并重建表单项 DOM，原占位符容器消失 ｜日志报错行 + 截图显示表单项结构已变 ｜`pages/network/slb/lb_pool.py:142 lb_forward_rule_create`，`.el-form-item` 占位符过滤 → `.form-left/.form-right` 区域定位 ｜Element UI 表单 + Vue v-if 动态渲染；适用：转发规则、策略配置等动态表单 ｜用 `.form-left` 区域定位替换占位符过滤：区域类名最稳定、不依赖 v-if 渲染时序，优于"选值前先存引用/改 get_by_role"两个候选 ｜含 v-if 动态切换的表单，禁止依赖会消失的占位符做容器定位，应用稳定区域/类名 ｜高·★★★★☆ ｜② 回填 phase_03 速查表「定位类」


