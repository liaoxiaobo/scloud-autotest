# 阶段四：日志分析与核查

---

## 一、执行提示词

````
阶段一产出的需求 MD 文件定义了本次任务的自动化测试需求，请完整分析 `sugon_web/logs/` 目录下所有测试文件的执行日志，不得遗漏任何文件。检查每个步骤的实际执行情况是否与该需求文档中定义的流程和预期结果一致。请以表格形式输出检查结果，字段包括但不限于：步骤序号、需求文档要求、实际执行的关键日志、测试结论（是否满足测试需求）。分析过程中严禁修改阶段一产出的 MD 需求文档，除非确认 MD 本身存在转换错误。
````

---

## 二、问题分级与处理规则

**使用说明：** 将检查结果与需求 MD 文档逐项对比，判断脚本是否满足预期测试需求。

- **重要问题（须返回阶段三修复）**：核心测试步骤遗漏、关键断言缺失、测试流程与需求严重不符。
- **非重要问题（可直接进入阶段五）**：日志格式不统一、Allure 步骤描述措辞差异、非关键字段值的小幅偏差。

**判断优先级（重要问题优先）：** 若日志中同时存在重要问题和非重要问题，**一律按重要问题处理**——返回阶段三修复脚本，非重要问题随修复过程一并处理；仅在**无重要问题**时，方可直接进入阶段五。

**完成标志：** 日志分析完成，每个步骤与需求一致，或已确认非重要问题并记录。

---

## 阶段四完成时立即输出（必须，不得延迟到 SKILL 总收尾）

**触发时机**：本阶段达到完成标志或被迫中断时,立即在对话框输出以下内容（不得跳过、不得合并到下一阶段）。

1. **状态标题**
   - 正常完成 → `阶段四任务已完成`
   - 被迫中断 → `阶段四任务被迫中断：{主要原因}`。如有深层根因则补充，无则省略。

2. **完成清单**

| 分析日志文件 | 需求覆盖度 | 重要问题数 | 核查结论 |
|---|---|---|---|
| `sugon_web/logs/test_xxx.log` | 完整/部分/未覆盖 | N | 通过/返回阶段三修复 |
