# 自愈报告模板（test-self-heal 总收尾·可选补充导出格式）

> **本文件定位（务必读准，防与 SKILL.md 混淆）**：本模板是**可选的补充导出格式**，用于需要另存一份独立摘要文件时参考；**它不是强制执行的规范**，也不替代 `SKILL.md`「完成后输出」一节——那一节才是本 Skill 实际执行、且被 `report_check.py --phase final` 机械校验的**权威总收尾格式**（1.总体结论 / 2.执行概览 / 3.风险与后续待办 / 4.产物位置索引 / 5.各阶段耗时统计），必须双写进运行报告主文件。本模板字段结构与之不同，仅供需要单独出一份对外摘要报告时按需取用，取用时以运行报告内已写入的权威内容为数据来源。
>
> 所有阶段完成后，先 `Read` 运行报告文件聚合生成本报告（不依赖对话记忆），在对话框输出，并可另存为 `skill_runs/test-self-heal/self-heal-report_{任务标识}_{YYYYMMDD_HHMM}.md`。

## 1. 任务概览

- 任务标识：`{任务标识}`
- 测试范围 / 入口：`{范围}` / `{full|diagnose|repair|regress|stabilize}`
- 前端版本基线：`{版本目录}`
- 运行报告文件：`skill_runs/test-self-heal/test-self-heal_{任务标识}_{YYYYMMDD_HHMM}.md`
- 总体结论：`稳定性验证通过 / 发现产品缺陷请提交 / 受环境影响建议优化 / 存在遗留问题`

## 2. 阶段产出表

| 执行阶段 | 关键产出 | 是否通过 |
|---|---|---|
| 阶段一 范围圈定+执行+失败采集 | 范围基线 + 模块→前端映射 + 失败清单 N 条 + 主因聚类 | 是 |
| 阶段二 4 分类分诊+前端对齐定位 | 4 分类结果 + 可自愈集合 + 疑似产品缺陷清单（无条件必出，无缺陷写"本次未发现"） | 是 |
| 阶段三 前端变更对齐与自愈修复 | 修复 N 个用例 | 是/部分 |
| 阶段四 回归验证与自愈循环 | 已自愈 N / 遗留 N | 是/部分 |
| 阶段五 稳定性验证与经验沉淀 | N 轮，通过 M 轮 | 是/不通过 |

## 3. 改动文件表

| 相对路径 | 新增行 | 删除行 | 改动摘要（依据） |
|---|---|---|---|
| `sugon_web/pages/xxx/xxx.py` | +N | -N | 定位 X 对齐新前端文案（Grep+recon 依据） |

## 4. 根因 4 分类统计

| 根因分类 | 数量 | 处理结果 |
|---|---|---|
| 前端变更（UI drift） | N | 已自愈 |
| 用例问题 | N | 已自愈 / 遗留 |
| 环境问题 | N | 已跳过记录 |
| 产品缺陷 | N | 已提示提交缺陷 |
| 待人工确认 | N | 待介入 |

## 5. 遗留问题清单

| 用例 nodeid | 遗留原因 | 建议人工介入方向 |
|---|---|---|

## 6. 修复经验沉淀

| 序号 | 所属阶段 | 问题简述 | 根因类型 | 修复建议 | 修复经验（可复用规律） | 适用场景 |
|:---:|:---:|:---|:---|:---|:---|:---|

## 7. 疑似产品缺陷提示（如有，字段与阶段二「疑似产品缺陷清单」一致，逐条列出）

> 无产品缺陷时本节写「本次未发现疑似产品缺陷」即可（与阶段二完成表口径一致，不留空）。

- 缺陷用例（nodeid）：`{用例 nodeid，如 test_xxx.py::TestX::test_y}`
- 失败现象：`{报错/异常摘要}`
- 为何判产品缺陷：`{前端对齐已排除、界面/后端行为本身异常的判定依据}`
- 复现要点：`{复现步骤}`
- 后端取证证据：`{scli/SSH 输出等，判定产品缺陷的关键依据，不可缺失}`
- 建议：请人工核实并提交缺陷单至缺陷管理系统。
