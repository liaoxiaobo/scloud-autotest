# 测试用例：<功能点/场景名>

## 用例名称
<模块>-<功能>-<场景/变体>

## 用例类型
<basic | scenario | exception>

## 适用模块
- 一级模块：network
- 二级模块：<如 VPC / 安全组>
- 三级模块：<可选，填写更细分的资源或页面>

## 适配范围

### 存储类型策略
- 包含存储类型：<all / xstor / usan / ceph / local / nfs / zbs / xbd / ustor ...>
- 跳过存储类型：<无 / local / usan,nfs ...>
- 说明：
  - 若仅支持部分存储类型，代码侧优先使用 `@only_stor(...)`
  - 若仅排除部分存储类型，代码侧优先使用 `@skip_stor(...)`
  - 若网络场景本身与存储无强绑定，填写 `all`

### 架构/节点限制
- 架构限制：<无 / 跳过 aarch64 / 仅 x86_64 ...>
- 节点要求：<无 / 至少 2 节点>
- 说明：如需跳过，注明对应原因与代码实现方式，例如 `@skip_arch(...)`、`@skip_if_nodes_less_than(...)` 或运行时 `pytest.skip(...)`


## 测试步骤

1. <步骤1名称>
   - 操作: <执行主操作，如创建/编辑/绑定/解绑/关联/搜索/删除等>
   - 预期: <操作成功，成功提示符合预期>

2. <步骤2名称>
   - 操作: <校验列表页结果>
   - 预期: <列表字段、状态、描述、关联关系正确>

3. <步骤3名称>
   - 操作: <校验详情页结果>
   - 预期: <详情字段、页签数据、默认规则正确>

4. <步骤4名称>
   - 操作: <校验关联效果，如连通性、规则生效、访问控制、监听转发或资源占用关系>
   - 预期: <实际效果与预期一致>

