# 测试用例：<功能点/场景名>

## 所属模块
- 一级模块：<如网络 / 存储 / 数据库>
- 二级模块：<如 VPC / 安全组>
- 三级模块：<可选，填写更细分的资源或页面>

## 适配范围

### 存储适配
- 该用例不支持的存储：<无 / local / usan,nfs ...>
- 说明：代码侧使用 `@skip_stor(...)`

### 架构/节点限制
- 架构限制：<无 / 仅 aarch64 / 仅 x86_64 ...>
- 节点要求：<无 / 至少 2 节点>
- 说明：代码侧使用 `@skip_arch(...)`、`@skip_if_nodes_less_than(...)`


## 测试步骤

1. 操作
   - 操作：<执行主操作，如创建/编辑/绑定/解绑/关联/搜索/删除等>
   - 预期：<操作成功，成功提示符合预期>

2. 操作
   - 操作：<校验列表页结果>
   - 预期：<列表字段、状态、描述、关联关系正确>

3. 操作
   - 操作：<校验关联效果，如连通性、后台规则生效>
   - 预期：<实际效果与预期一致>


## 推荐填写示例

```md
# 测试用例：对等连接-创建和删除

## 所属模块
- 一级模块：网络
- 二级模块：对等连接

## 适配范围

### 存储适配
- 该用例不支持的存储：无

### 架构/节点限制
- 架构限制：无
- 节点要求：无

## 测试步骤

1. 操作
   - 操作：预置 2 个可用 VPC，分别作为本端和对端
   - 预期：2 个 VPC 创建成功并处于可用状态

2. 操作
   - 操作：进入对等连接页面，点击新建，填写名称、本端 VPC、对端 VPC、描述后提交
   - 预期：对等连接创建成功

3. 操作
   - 操作：在列表页查看新建的对等连接
   - 预期：名称、本端 VPC、对端 VPC、描述正确，状态正常



# 测试用例：弹性云服务器-克隆

## 所属模块
- 一级模块：计算
- 二级模块：弹性云服务器

## 适配范围

### 存储适配
- 该用例不支持的存储：无

### 架构/节点限制
- 架构限制：无
- 节点要求：无

## 测试步骤

1. 操作
   - 操作：预置一台云服务器，登录并在系统盘 `/data` 路径写入测试数据，执行 md5sum 命令记录该文件的 MD5
   - 预期：测试文件写入成功，可获取到源文件 MD5 值

2. 操作
   - 操作：在弹性云服务器列表中，对该实例执行克隆操作
   - 预期：克隆成功，实例的状态处于可用状态

3. 操作
   - 操作：登录克隆实例，执行 md5sum 命令，计算出 `/data` 文件的 MD5，并与源实例记录值比对
   - 预期：克隆实例中的文件 MD5 与源实例一致

```

