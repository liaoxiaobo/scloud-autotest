# 测试用例：裸金属BMS-软装版-查看监控

## 基本信息
- 用例编号：BMS_003
- 所属产品：曙光云Stack
- 一级模块：计算
- 二级模块：裸金属BMS-软装版

## 适配范围

### 存储适配
- 该用例不支持的存储：无

### 架构/节点限制
- 架构限制：无
- 节点要求：无

## 前置条件

1. 已存在软装版裸金属实例 `bms-0430`，状态为"运行中"
   - 本资源在多个场景中复用，测试结束后不清理
2. 裸金属实例密码：`admin1234@sugon`

## 测试步骤

### 场景1：查看裸金属实例监控

#### 步骤1：搜索裸金属实例

- 操作：进入"产品与服务→计算→裸金属BMS-软装版→裸金属实例"页面，搜索 "bms-0430"
- 预期：列表中可以搜索到名为"bms-0430"的实例

#### 步骤2：查看监控

- 操作：在实例列表中，点击实例"bms-0430"操作栏的"查看监控"
- 预期：弹出监控页面/对话框

#### 步骤3：验证CPU和内存使用率

- 操作：在监控页面中查看CPU使用率和内存使用率(%)
- 预期：
  - CPU使用率有数值显示（非空）
  - 内存使用率有数值显示（非空）

## ⚠️ 实现注意事项

### 1. Fixture强制约束
- 实现测试步骤或前置条件的自动化脚本前，**必须**阅读 `sugon_web/case_specs/fixtures_index.md`
- 查阅fixture强制约束和速查表，确认框架中是否已有可复用的fixture
- **禁止**针对已有的fixture进行重复封装

### 2. 可复用Fixture参考

根据 `fixtures_index.md`，本用例可能涉及的fixture：

- `bms_page` fixture：裸金属BMS页面对象（function级别）

### 3. 监控页面特殊说明
- "查看监控"操作在操作栏直接可见（不是"更多"下拉菜单内），或通过"更多"下拉菜单访问，需根据实际前端实现确定
- 监控数据可能需要等待加载，建议增加适当等待时间
- CPU和内存使用率验证只需确认有数值（非空），不强制具体数值范围

## 清理数据

本用例为只读查看操作，不涉及数据修改，无需额外清理。

## 推荐填写示例

（略，参考 base_case.md 标准模板）
